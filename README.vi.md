# 🚀 AugmentCode Unlimited

**🏆 Giải pháp hàng đầu để bỏ qua giới hạn thiết bị AugmentCode 🏆**

*✅ **Hoàn toàn miễn phí** | 🚫 **Không cần đăng ký** | 🎯 Hỗ trợ 15+ IDE phổ biến | 🧠 Dọn dẹp thông minh 4 lớp | 🛡️ Bảo mật cấp doanh nghiệp*

**🚀 Dọn dẹp siêu tốc 3 giây | 📊 100% độ hoàn chỉnh dọn dẹp | 🔒 99.9% độ an toàn dữ liệu**


-----

### 🌟 Tại sao chọn AugmentCode Unlimited?

Trong số nhiều giải pháp bỏ qua giới hạn AugmentCode, **AugmentCode Unlimited** là công cụ duy nhất cung cấp **dọn dẹp chính xác 4 lớp hoàn chỉnh**, **backup bảo mật cấp doanh nghiệp** và **sử dụng một cú nhấp không cần cấu hình**. Quan trọng nhất, nó **hoàn toàn miễn phí, không cần đăng ký**, cam kết mang đến trải nghiệm lập trình AI thuần túy và tự do nhất cho mọi nhà phát triển. Chúng tôi không chỉ giải quyết vấn đề, mà còn quan tâm đến bảo mật dữ liệu và trải nghiệm sử dụng của bạn.

#### Ưu điểm cốt lõi

| **🎯 Bao phủ toàn diện** | **🧠 Phát hiện thông minh** | **⚡ Siêu tốc hiệu quả** | **🛡️ Bảo mật cấp doanh nghiệp** |
| :--- | :--- | :--- | :--- |
| Hỗ trợ toàn bộ JetBrains, VSCode, Cursor, VSCodium và hơn 15 IDE phổ biến, một công cụ giải quyết tất cả. | Tự động nhận diện phiên bản IDE, hiển thị nội dung cụ thể cần dọn dẹp, mọi thao tác đều rõ ràng. | Hoàn tất dọn dẹp cốt lõi trong 3-5 giây, thông qua xử lý song song và phát hiện tăng dần, nhanh hơn 3 lần so với công cụ tương tự. | Backup thông minh, dọn dẹp chính xác, khóa file, khôi phục một cú nhấp, bảo vệ an toàn dữ liệu của bạn. |

> ⚠️ **Tuyên bố miễn trừ trách nhiệm**: Dự án này chỉ dành cho mục đích học tập và nghiên cứu, **hoàn toàn miễn phí**. Vui lòng sử dụng tuân thủ các điều khoản sử dụng phần mềm liên quan và pháp luật địa phương. Người dùng tự chịu mọi rủi ro.

-----

### 🔥 So sánh với các dự án tương tự

| Tính năng | **AugmentCode Unlimited** | `project-a` (Rust) | `project-b` (Python) | `project-c` (Python) |
| :--- |:--------------------------| :--- | :--- | :--- |
| **Giá và đăng ký** | ✅ **Hoàn toàn miễn phí, không cần đăng ký**           | ❓ Không rõ/có thể tính phí | ❓ Không rõ/cần đăng ký | ❓ Không rõ |
| **Phạm vi hỗ trợ IDE** | ✅ **Tất cả IDE phổ biến**             | ⚠️ Chỉ JetBrains+VSCode | ⚠️ Chỉ VSCode | ❌ Chỉ Cursor |
| **Khả năng bỏ qua giới hạn** | ✅ **Dọn dẹp 4 lớp hoàn chỉnh**              | ⚠️ Dọn dẹp một phần | ⚠️ Dọn dẹp cơ bản | ❌ Dọn dẹp một lớp |
| **Giao diện đồ họa (GUI)** | ✅ **GUI chuyên nghiệp+trạng thái thời gian thực**          | ❌ Chỉ dòng lệnh | ❌ Chỉ dòng lệnh | ✅ GUI cơ bản |
| **Phát hiện và hiển thị thông minh** | ✅ **Hiển thị mục dọn dẹp cụ thể**             | ❌ Không có thông tin chi tiết | ❌ Không có thông tin chi tiết | ⚠️ Phát hiện cơ bản |
| **Backup và khôi phục an toàn** | ✅ **Backup thông minh+khôi phục một cú nhấp**           | ⚠️ Backup cơ bản | ⚠️ Backup cơ bản | ❌ Không có backup |
| **Quản lý phụ thuộc** | ✅ **Cài đặt tự động một cú nhấp**              | ⚠️ Cài đặt thủ công | ⚠️ Cài đặt thủ công | ⚠️ Cài đặt thủ công |
| **Bảo vệ khóa file** | ✅ **Bảo vệ chống ghi đè**               | ✅ Hỗ trợ | ❌ Không hỗ trợ | ❌ Không hỗ trợ |
| **Quản lý tiến trình tự động** | ✅ **Tự động đóng IDE**             | ❌ Cần đóng thủ công | ❌ Cần đóng thủ công | ⚠️ Hỗ trợ một phần |
| **Hỗ trợ đa nền tảng** | ⚠️ **Windows**              | ✅ Hỗ trợ | ⚠️ Chủ yếu Windows | ⚠️ Chủ yếu Windows |
| **Ngôn ngữ và khả năng bảo trì** | 🐍 **Python (dễ bảo trì mở rộng)**    | 🦀 Rust (biên dịch, khó bảo trì) | 🐍 Python | 🐍 Python |

-----

### ✨ Chi tiết tính năng cốt lõi

#### 🛡️ Tính năng bảo mật cấp doanh nghiệp

* **Backup thông minh**: Tự động tạo backup có timestamp trước mọi thao tác dọn dẹp. GUI hỗ trợ duyệt backup và **khôi phục một cú nhấp**.
* **Dọn dẹp chính xác**: **Chỉ dọn dẹp dữ liệu liên quan AugmentCode**, tuyệt đối không chạm vào cấu hình IDE và dữ liệu plugin khác, bảo vệ hoàn hảo môi trường phát triển của bạn.
* **Khóa file**: Sau khi dọn dẹp Device ID có thể tự động khóa file liên quan, ngăn IDE tự động khôi phục.
* **Xác minh tính toàn vẹn**: Đảm bảo tính toàn vẹn và an toàn của thao tác dọn dẹp, có thể tự động rollback khi có lỗi.
* **Quản lý tiến trình**: Tự động phát hiện và yêu cầu đóng tiến trình IDE liên quan trước khi dọn dẹp, tránh file bị chiếm dụng.

#### 🎨 Trải nghiệm người dùng xuất sắc

* **Khởi động không cần cấu hình**: Cung cấp script khởi động một cú nhấp, tự động kiểm tra và cài đặt phụ thuộc cần thiết.
* **Giao diện GUI chuyên nghiệp**: Hiển thị thời gian thực trạng thái cài đặt IDE được phát hiện và nội dung cụ thể cần dọn dẹp, quá trình thao tác rõ ràng.
* **Nhiều cách triển khai**: Hỗ trợ chạy trực tiếp mã nguồn Python, cũng hỗ trợ đóng gói thành **file exe độc lập hoàn toàn miễn phí, không cần cài đặt, không phụ thuộc**.
* **Ghi log chi tiết**: Tạo log thao tác hoàn chỉnh trong thư mục `logs/`, thuận tiện cho việc xử lý sự cố.

#### 🧠 Phát hiện thông minh và hiển thị trạng thái

Trước khi dọn dẹp, công cụ sẽ quét thông minh hệ thống của bạn và hiển thị rõ ràng các thao tác sẽ thực hiện, ví dụ:

```log
🆔 Dấu vân tay thiết bị: ⚠️ 8 file cần cập nhật
   • JetBrains deviceId: a1b2c3d4...
   • VSCode machineId: e5f6g7h8...
   • Cursor storage.json: machineId=i9j0k1l2..., deviceId=m3n4o5p6...

🗃️ Bản ghi OAuth: ⚠️ 15 bản ghi cần dọn dẹp
   • VSCode state.vscdb: Phát hiện 8 bản ghi liên quan
   • Cursor state.vscdb: Phát hiện 7 bản ghi liên quan

📁 Thư mục workspace: ⚠️ 3 thư mục cần dọn dẹp
   • VSCode: Phát hiện 12 dự án (trong đó 5 dự án có dấu vết sử dụng AugmentCode)
   • Cursor: Phát hiện 8 dự án (trong đó 3 dự án có dấu vết sử dụng AugmentCode)
```

-----

### 📦 Cài đặt và sử dụng

#### 🚀 Phương pháp 1: Tải xuống file thực thi Release (Khuyến nghị, đơn giản nhất)

1.  Truy cập trang [Releases] của dự án này.
2.  Tải xuống phiên bản mới nhất của `AugmentCode-Unlimited.exe`.
3.  **Hoàn toàn miễn phí, không cần đăng ký, giải nén và sử dụng ngay**.

#### 🛠️ Phương pháp 2: Chạy từ mã nguồn (Phù hợp cho nhà phát triển)

1.  **Clone dự án**
    ```bash
    git clone https://github.com/wozhenbang2004/augetment-cursor-unlimited.git
    cd augetment-cursor-unlimited
    ```
2.  **Khởi động một cú nhấp (Windows)**
    * Nhấp đúp `start.bat`.
    * Script sẽ tự động tạo môi trường ảo, cài đặt phụ thuộc và khởi động chương trình.
3.  **Chạy thủ công (Tất cả nền tảng)**
    ```bash
    # (Tùy chọn, khuyến nghị) Tạo và kích hoạt môi trường ảo Python
    python -m venv venv
    source venv/bin/activate  # trên Linux/macOS
    venv\Scripts\activate     # trên Windows

    # Cài đặt phụ thuộc
    pip install -r requirements.txt

    # Chạy GUI
    python gui_main.py

    # Hoặc chạy phiên bản dòng lệnh
    python main.py --help
    ```

-----

### 🚀 Hướng dẫn sử dụng

#### Thao tác giao diện đồ họa (GUI)

1.  **Khởi động**: Chạy trực tiếp file `.exe` hoặc script `start.bat`.
2.  **Phát hiện**: Sau khi khởi động, chương trình sẽ tự động phát hiện IDE và dữ liệu liên quan trong hệ thống, và hiển thị trên giao diện.
3.  **Dọn dẹp**: Nhấp nút **"🚀 Bắt đầu dọn dẹp"**, chương trình sẽ tự động hoàn tất tất cả thao tác.
4.  **Khởi động lại IDE**: Sau khi dọn dẹp hoàn tất, đóng công cụ này, khởi động lại IDE của bạn (như Cursor, VSCode), sau đó có thể **đăng nhập bằng tài khoản mới**.
5.  **(Tùy chọn) Khôi phục**: Nếu gặp vấn đề, có thể nhấp nút "Khôi phục backup", chọn backup trước đó để khôi phục một cú nhấp.

#### Thao tác dòng lệnh (CLI) (Người dùng nâng cao)

```bash
# Xem thông tin hệ thống và mục cần dọn dẹp
python main.py --info

# Chỉ xử lý IDE cụ thể
python main.py --jetbrains-only  # Chỉ xử lý JetBrains
python main.py --vscode-only      # Chỉ xử lý VSCode series

# Tùy chọn nâng cao
python main.py --no-backup       # Bỏ qua backup (nhanh hơn nhưng có rủi ro)
python main.py --verbose         # Xuất log chi tiết hơn
python main.py --help            # Xem hướng dẫn đầy đủ
```

-----

### 🔧 Nguyên lý hoạt động: Công nghệ dọn dẹp chính xác 4 lớp độc quyền

AugmentCode thực hiện giới hạn thiết bị thông qua bốn lớp sau, công cụ của chúng tôi đã thiết kế chiến lược bỏ qua chính xác cho từng lớp:

1.  **Nhận diện dấu vân tay thiết bị (Device Fingerprinting)**
    * **Cơ chế giới hạn:** Ghi ID thiết bị duy nhất vào file cấu hình IDE (như `storage.json`, `PermanentDeviceId`).
    * **Chiến lược bỏ qua:** **Đặt lại dấu vân tay thiết bị**. Tạo ID ngẫu nhiên mới phù hợp định dạng yêu cầu, và thay thế ID cũ.
2.  **Theo dõi database toàn cục (Global Database Tracking)**
    * **Cơ chế giới hạn:** Lưu trữ bản ghi OAuth, token phiên và thông tin xác thực trong database toàn cục (như `state.vscdb`).
    * **Chiến lược bỏ qua:** **Dọn dẹp database chính xác**. Chỉ xóa các cặp khóa-giá trị liên quan AugmentCode, không ảnh hưởng dữ liệu plugin khác.
3.  **Giới hạn ràng buộc workspace (Workspace Binding)**
    * **Cơ chế giới hạn:** Ghi lại lịch sử sử dụng và trạng thái ràng buộc trong lưu trữ workspace của dự án (`workspaceStorage`).
    * **Chiến lược bỏ qua:** **Dọn dẹp workspace không mất dữ liệu**. Duyệt từng workspace, chỉ dọn dẹp dữ liệu AugmentCode liên quan, **hoàn hảo giữ lại cấu hình cấp dự án của người dùng cho tất cả plugin khác**.
4.  **Theo dõi dấu vân tay mạng (Network Fingerprinting)**
    * **Cơ chế giới hạn:** Theo dõi phiên mạng thông qua cache trình duyệt, Cookie, v.v.
    * **Chiến lược bỏ qua:** **Cung cấp gợi ý an toàn**. Công cụ mặc định không chạm vào dữ liệu trình duyệt để tránh phá hủy môi trường, nhưng trong tùy chọn nâng cao cung cấp hướng dẫn phương pháp dọn dẹp dữ liệu liên quan trình duyệt một cách thủ công và an toàn.

-----

### 🛡️ Tính năng bảo mật

* **Backup tự động và khôi phục một cú nhấp**: Trước mọi thao tác ghi, tự động tạo backup có timestamp trong thư mục `~/.augment_unlimiter_backups/`. Hỗ trợ khôi phục một cú nhấp về bất kỳ điểm backup lịch sử nào trong GUI.
* **Quản lý quyền và khóa file**: Tự động xử lý các vấn đề quyền như file chỉ đọc, và có thể khóa file ID quan trọng sau khi dọn dẹp, ngăn IDE tự động khôi phục.
* **Thao tác nguyên tử và rollback**: Quá trình dọn dẹp được thiết kế như thao tác nguyên tử, nếu xuất hiện lỗi nghiêm trọng giữa chừng, sẽ thử tự động rollback các sửa đổi đã thực hiện, đảm bảo tính nhất quán dữ liệu.

-----

### 🔧 Xử lý sự cố

* **Lỗi quyền**: Trên Windows, vui lòng thử nhấp chuột phải vào file `.exe` hoặc `.bat`, chọn "Chạy với tư cách quản trị viên".
* **Không phát hiện IDE**: Vui lòng đảm bảo IDE của bạn được cài đặt theo đường dẫn tiêu chuẩn. Có thể chạy `python main.py --info` để xem đường dẫn phát hiện của công cụ.
* **Dọn dẹp thất bại**: Vui lòng đảm bảo đã đóng hoàn toàn tất cả tiến trình IDE liên quan trước khi thực hiện dọn dẹp. Xem file log trong thư mục `logs/` để lấy thông tin lỗi chi tiết.

-----

### 🤝 Đóng góp và hỗ trợ

Chào mừng gửi **Issue** để báo cáo vấn đề hoặc đưa ra đề xuất, cũng chào mừng tham gia cải thiện dự án trực tiếp thông qua **Pull Request**!


### 🌟 Lịch sử Star

Nếu dự án này hữu ích cho bạn, vui lòng cho một ⭐ để ủng hộ! Star của bạn là động lực lớn nhất để chúng tôi tiếp tục cải thiện!

https://github.com/wozhenbang2004/augetment-cursor-unlimited.git

-----

<div align="center">

**⭐ Nếu thấy hữu ích, vui lòng cho một Star để ủng hộ! Đây là công cụ mã nguồn mở hoàn toàn miễn phí! ⭐**

**🔥 Để nhiều nhà phát triển hơn được hưởng lợi, cùng nhau tạo ra giải pháp AugmentCode mạnh nhất! 🔥**

Made with ❤️ by [Kayokoi](https://github.com/wozhenbang2004)

</div>

